<UserControl x:Class="Rps.Client.Views.MainMenuWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Image Source="/Media/Images/RpsSpaceGameLogo.png" Grid.Column="1" />
        <StackPanel Margin="20 10 0 0" Grid.Row="1" Grid.Column="1" Orientation="Vertical">
            <Button x:Name="StartButton" Width="125" Height="50" Margin="0" BorderBrush="Transparent" BorderThickness="100" Click="StartButton_Click">
                Start Game
            </Button>
            <Button x:Name="HighScoreButton" Width="125" Height="50" Margin="0"  BorderBrush="Transparent"  BorderThickness="100" Click="HighScoreButton_Click">
                Highscores
            </Button>
            <Button x:Name="OptionButton" Width="125" Height="50" Margin="0"  BorderBrush="Transparent"  BorderThickness="100" Click="OptionButton_Click">
                Options
            </Button>
            <Button x:Name="QuitButton" Width="125" Height="50" Margin="0"  BorderBrush="Transparent"  BorderThickness="100" Click="QuitButton_Click">
                Quit
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
